 <!-- booking section starts -->
 <?php 
    $schedule = $data['schedule'];
    $availableSeats = $data['availableSeats'];
?>

 <section class="home container booking mt-5">
    <div class="content">
        <h1 class="heading title mb-5">Reservation Form</h1>
        <?= show($schedule); ?>

        <form action="../../Book_Confirm/save" method="post" class="book-form">

            <div class="flex">

                <div class="inputBox">
                    <span>Date :</span>
                    <input type="date" name="reserveDate" value="<?= $data['date'] ?>" readonly>
                </div>

                <div class="inputBox">
                        <span>Bus # :</span>
                        <input type="text" value="<?= $schedule->bus_number ?> " name="busNo" readonly>
                </div>

                <div class="inputBox">
                        <span>Location :</span>
                        <input type="text" value = "<?= $schedule->location ?> " name="location" readonly>
                </div>
                
                <div class="inputBox">
                        <span>Departure :</span>
                        <input type="time" value="<?= $schedule->dept_time ?>" name="departure" readonly>
                </div>
                
                <div class="inputBox">
                    <span>Arrival :</span>
                    <input type="time" value="<?= $schedule->arrival_time ?>" name="arrival" readonly>
                </div>
                
                <div class="inputBox">
                    <span>Price :</span>
                    <input type="int" value="<?= $schedule->price ?> " name="price" readonly>
                </div>
                
                <div class="inputBox">
                    <span>Passenger Name :</span>
                    <input type="text" name="passengerName">
                </div>
                
                <div class="inputBox">
                    <div class="mb-2">Seat Number:</div>
                    <select class="form-select" aria-label="Default select example" name="seatNo">
                        <option selected>Seat Number</option>
                        <?php foreach ($availableSeats as $value) : ?>
                            <option value=" <?= $value ?>"> <?= $value ?> </option>
                        <?php endforeach; ?>
                    </select>
                </div>
                
                <div class="inputBox">
                    <div class="mb-2">Payment:</div>
                    <select class="form-select" aria-label="Default select example" name="paymentMethod">
                        <option selected>Payment Method</option>
                            <option value="gcash"> GCash </option>
                            <option value="atm"> Visa/Mastercard/Bancnet </option>
                    </select>
                </div>

                <div class="">                    
                    <input type="text" value="<?= $schedule->sched_id ?> " name="schedID" hidden>
                </div>

                <div class="inputBox"></div>
                <div class="inputBox"></div>

            </div>
            <br>
            <button type="submit" class="btn btn-outline-secondary btn-lg">Submit</button>
        </form> 
    </div>            
</section>
        <!-- booking section ends -->